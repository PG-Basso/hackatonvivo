<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thiago.muitogelo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CadastroActivity" />
        <activity android:name=".CadastroImovel1Activity" />
        <activity android:name=".CadastroImovel2Activity" />
        <activity android:name=".CadastroImovel3Activity" />
        <activity android:name=".PrincipalActivity" />
        <activity android:name=".FAQActivity" />
        <activity android:name=".SaibaMaisActivity" />
        <activity android:name=".PremiosActivity"/>
    </application>

</manifest>